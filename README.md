# Clone the repository
```bash
git clone https://github.com/nisarg-110/FitnessApp.git
cd FitnessApp

# Setup MongoDB (update connection string in backend/config/db.js if using MongoDB Atlas)

# Run the application
cd backend
npm install 
npm start

cd fitness-scheduler
npm install
ng serve

"Application running:"
"Frontend: http://localhost:4200"
"Backend: http://localhost:3000"
